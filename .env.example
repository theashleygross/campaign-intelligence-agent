# Copy this file to .env to configure the app.

# When true (default), the app runs entirely on deterministic logic --
# no network calls, no API key required.
MOCK_MODE=true

# Required only when MOCK_MODE=false. Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
